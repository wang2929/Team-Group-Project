# Placeholder import because will import main-menu later
from main_menu import main_menu

# Putting main here, though in main-menu also works, just because could be better design-wise
if __name__ == '__main__':
    main_menu()